/** ==========================================================
* NAME: 
============================================================ */
/**@DESC: xxxxxxxxxx */
const {app, port} = require("./server/server");
//# XXXXX >
app.listen(port, () => { console.log(`On Port: ${ port }`) });